image:
  file: .gitpod.dockerfile

tasks:
  - name: go-install
    init: |
      gvm use go1.13
      #go get -v -t -d ./...
    
ports:
  - port: 8080
    visibility: public

vscode:
  extensions:
    - golang.go
    - wayou.vscode-todo-highlight
    - coenraads.bracket-pair-colorizer-2
